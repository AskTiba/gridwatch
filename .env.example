# Local (Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/municipal_utility_monitor

# Production (Supabase) — copy from Supabase Dashboard > Settings > Database
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:admin@municipal-utility.local
